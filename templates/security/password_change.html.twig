{% include 'default/_form.html.twig' with {
    page_title: 'action.password_change'|trans,
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('app_change-password', {'id': user.id}),
    },
    submit_label: 'action.save'|trans,
    back_to_list_path: 'book_index',
} %}